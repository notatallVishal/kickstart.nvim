# `vishal.`'s work-in-progress nvim config

## Some Reference for myself

- `ctrl-q` enters visual block mode. sorround a text and do `:VBox` to activate venn.nvim
```c 
/*    ┌──────────────────────┐
*     │This is my epic block │
*     └──────────────────────┘
*/
```

- `s` in normal mode lets you do flash.

- try to use fuzzy find (`leader space`) over neo-tree
